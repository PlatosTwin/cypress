# Reviewers: nothing here reaches the app. The three files changed are Tools/run_tests.sh,
# Tools/verify_test_log.sh and Tools/fetch_seed.sh, plus a new Tools/test_harness_guards.sh.
internal: bounds the simulator boot, stops the collision guard matching its own caller, and gives an environment-refused run its own verdict; no app code changed.
