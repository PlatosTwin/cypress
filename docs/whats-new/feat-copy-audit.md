# One note for both halves of the copy audit — the kills (phase 2A) and the rewrites (phase 2B)
# ship in this one pull request, so they are one line to a tester.
#
# It is deliberately about what a reader sees rather than about the audit: 13 strings deleted, 23
# rewritten and 12 footnote slots removed is a count, and a tester does not read counts. What they
# meet is a quieter app whose controls describe themselves.
#
# True at merge: every string named here is changed on this branch and the suite has run on the
# merged tree. Nothing below is behind a flag and nothing waits on a publish.

The wording across the app is plainer. Buttons and notices now say what they do, and the small commentary lines at the foot of many screens are gone.
