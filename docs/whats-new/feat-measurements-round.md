Switching units while measuring keeps the number you typed — a note under the field tells you how it now reads instead of clearing your entry.
Trees that already have both a height and a trunk reading now show an Add a reading link on their profile, so a new reading is one tap instead of two screens away.
