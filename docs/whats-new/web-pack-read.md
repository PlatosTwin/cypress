# `internal:` and not the obvious sentence. This reads a published city pack the same way the phone
# does, but nothing calls it: the page is milestone W-C, unwritten, and the layer is read-only by
# construction. No Swift changed and no build is minted. When a page renders a tree from a pack on
# a deployed service, that is tester-visible and earns its own note.

internal: the web can open a published city pack read-only through the same schema the phone uses. No page reads it yet.
