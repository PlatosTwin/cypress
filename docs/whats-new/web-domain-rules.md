# `internal:` and not the obvious sentence. These are the phone's own rules re-derived in
# TypeScript and checked against the Swift's own test cases — so by construction they change no
# behavior anyone can see. Nothing calls them yet (the page is W-C), and no Swift changed.
#
# One thing found here IS tester-relevant and is deliberately NOT claimed: the 25 m public-photo
# grid is not idempotent and the read path applies it twice. No code changed, because two of the
# three candidate fixes move already-published coordinates — it is the owner's decision, recorded
# unnumbered in docs/errata-pending/. Saying anything here would describe a fix that did not ship.

internal: the vitality rubric, Quantity, the 25 m grid, growth-charting eligibility and the ID spaces are re-derived in TypeScript against the Swift's own cases.
