# Tester-voice note for the neighborhood/city stats picker (R81).
# Comment lines and blank lines are ignored by `Tools/whats_new.py compile`.
# Two lines, each under the 200 characters `check` allows: the picker, and the coarse-fix state.

In the Journal, the Neighborhood and City pages now say where their area came from, and a Change button lets you read any other neighborhood or city your phone's inventories cover.

When your phone's location is too rough to tell which neighborhood you're in, the page says so instead of naming one anyway.
