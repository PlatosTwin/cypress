# `internal:`, and the sentence is worth stating plainly rather than hiding behind the prefix.
#
# W1 now RENDERS — a real tree, out of the real pinned seed, at `/‹id-space›/tree/‹uuid›`, with the
# OpenGraph card its caption specifies. That is a page a person can look at, and by the ordinary
# reading of R81 it is the most tester-visible thing this round could produce.
#
# It is `internal:` anyway, because no tester can reach it. Nothing is deployed (W-E), `cypress.app`
# serves nothing, and `ShareCopy.publicURLPrefix` still points at a host with no A record — so every
# share card the app has ever produced is still a dead link, exactly as it was yesterday. No Swift
# changed, no build is minted, and the TestFlight notes would be describing a URL that 404s.
#
# The note that is not `internal:` is W-E's: the round that puts this on the internet is the round
# where the share link starts resolving, and that is the change a tester can see.

internal: the public tree page renders server-side from a published city pack, at the path the app's share cards have always pointed at. Nothing is deployed, so no link resolves yet.
