    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
N    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
O    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
3    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
<    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
4    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
B    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
B    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
4    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
’    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
’    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
N    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
O    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
M    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
’    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
B    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
O    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
Y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
z    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
z    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
z    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
z    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
z    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
z    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
N    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
6    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
8    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
3    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
3    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
9    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
7    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
9    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
N    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
6    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
8    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
3    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
3    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
9    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
7    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
9    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
3    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
D    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
N    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
N    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
V    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
I    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
N    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
"    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
V    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
B    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
·    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
–    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
%    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
·    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
6    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
–    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
%    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
—    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
L    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
3    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
|    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
V    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
#    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
5    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
0    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
+    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
U    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
C    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
V    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
O    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
x    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
*    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
F    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
!    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
E    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
[    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
]    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
j    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
+    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
?    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
+    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
+    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
A    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
B    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
O    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
H    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
B    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
/    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
P    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
-    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
>    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
=    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
2    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
_    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
q    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
g    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
1    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
T    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
W    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
S    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
R    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
u    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
d    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
(    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
n    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
f    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
w    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
.    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
y    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
'    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
p    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
h    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
:    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
\    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
b    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
c    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
k    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
o    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
m    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
s    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
$    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
{    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
r    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
l    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
a    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
t    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
i    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
v    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
e    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
`    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
,    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
     // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
}    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
)    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
;    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }

    // Both triggers, not one. `push:` and `pull_request:` carry separate filters and the one
    // that matters for a PR is the second; a list added to only the first reads as done. Counted
    // by occurrence rather than by splitting on `paths:`, because that word also appears in the
    // file's prose and a split would hand a comment to the loop above as if it were a filter.
    for (const relative of sourcesTheWebSuiteReads) {
      const occurrences = workflow.split(`'${relative}'`).length - 1;
      assert.ok(
        occurrences >= 2,
        `web.yml names ${relative} ${occurrences} time(s); it belongs in both the push and the `
          + `pull_request \`paths:\` filter, and a PR is judged by the second`,
      );
    }
